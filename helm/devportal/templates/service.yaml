apiVersion: v1
kind: Service
metadata:
  name: {{ include "devportal.fullname" . }}
  labels:
    {{- include "devportal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devportal.selectorLabels" . | nindent 4 }}
