# DEPRECATED — use Helm chart platform-config or:
#   kubectl create configmap platform-config \
#     --from-file=platform.yaml=../config/platform.yaml \
#     -n devportal-system
#
# Or install via: helm upgrade --install geeko-ops ./helm/geeko-ops
